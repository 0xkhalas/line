// ----------------------------------------------------------------------------
// main — the self-hosted compiler driver
// ----------------------------------------------------------------------------

std :: import("$std")

main :: fun(argc: i32, argv: &&u8) -> i32 {
	std.printf("Hello, World!\n");
	return 0;
}
