// ----------------------------------------------------------------------------
// Assert -
// ----------------------------------------------------------------------------

local std :: import("$std")

self :: fun(condtion: bool, message: &u8 = "assert failed") {
	if (!condtion) {
		panic(message);
	}
}
